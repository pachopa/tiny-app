<!DOCTYPE html>
<html lang="en">
<head>

  <% include partials/_header %>

</head>
<body>
  <ul>
  <% for(var key in urls) { %>

    <form action="urls/<%= key %>/delete"
    method="post">
    <li><%= key + ': ' + urls[key] %>
       <a href="http://localhost:8080/urls/<%= key %>">Edit</a>
       <button type="submit"> Delete</button>

       </form>
    </li>
    <% } %>


 </ul>

</body>
</html>